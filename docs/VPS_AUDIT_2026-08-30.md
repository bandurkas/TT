# VPS3: проверка перед продолжением разработки

Исторический аудит до изменений интерфейса. После этой проверки коммиты `22b1929` и `fe1b2a5` были развёрнуты через Git; актуализация — в [development readiness](DEVELOPMENT_READINESS.md). Выявленный ниже риск доступа без авторизации остаётся открытым.

Проверено 2026-08-30 около 20:01–20:06 UTC через SSH и внешние HTTP-запросы.
Хост: `187.127.114.34`, Ubuntu 24.04.4 LTS. Проект: `/root/TT`.
При первом подключении принят ED25519 fingerprint `SHA256:usdHAC+Bv/iFLXLAe8cf985pAKzsS+vN9fbewUBq2c0`; независимая сверка через консоль провайдера не выполнялась.
Пароль и содержимое секретов в файлах аудита не сохранялись.

## Подтверждённое состояние

- Рабочая копия VPS чистая, main / origin/main на `3b7f0e8`, соответствует исходной локальной копии.
- api, dashboard, worker, caddy, postgres, redis запущены. PostgreSQL healthy.
- `/health`: status=ok, db=ok; finance_cycle и daily_metrics success, stale=false. Метрики обновились около 20:00 UTC.
- Версия миграции БД: `a9c4e7f1b2d3`, совпадает с source head.
- TikTok Ads App ID и Secret не настроены. Статус approval в кабинете TikTok не проверялся.
- Система сообщает о требуемой перезагрузке и доступных обновлениях. Обновления и перезагрузка не выполнялись.

## Проверки backend

Проверен образ `tt-api`, image ID `84ffe5c81207`, Python 3.12.14.
В образе есть исходники и tests, файла `/app/.env` нет.
Зависимости pytest/pytest-asyncio/ruff скачаны отдельным одноразовым контейнером.
Тестовый контейнер запущен с `--network none`, ограничением CPU/RAM, без production env, без томов данных, с read-only каталогом wheel-пакетов.

Результат: **339 passed**, 1 предупреждение Starlette о deprecated httpx TestClient; **Ruff: All checks passed**.
Это проверка unit-набора в Linux на основе deployed API image, а не полная end-to-end проверка TikTok или интерфейса.
Оба временных контейнера удалены автоматически (`--rm`). Каталог wheel-пакетов `/tmp/tt-audit-*` оставлен для повторных проверок; секретов в нём нет.

## Критичный риск доступа

Без cookies и credentials из внешней сети получены:

| URL | Результат |
| --- | --- |
| `https://tt.lomiraproduct.com/` | 200 HTML |
| `https://tt.lomiraproduct.com/api/dashboard/overview?from=2026-08-01&to=2026-08-30` | 200 JSON, включая cards/unit_economics/totals |
| `http://187.127.114.34:8400/health` | 200 |
| `http://187.127.114.34:3400/` | 200 HTML |

Caddyfile не требует авторизацию. API и dashboard опубликованы на 0.0.0.0 / IPv6. UFW inactive. Фактический доступ к финансовому API без входа подтверждён, а не только предположен по коду.
В исходниках task POST/PATCH также не имеют проверки пользователя; мутации на production намеренно не проверялись.
OAuth callbacks не проверяют state; запросы с кодами авторизации в ходе аудита не выполнялись.

## Следующий рабочий блок

1. Подготовить защиту dashboard/API, включая health, docs и OAuth status; выбрать механизм входа. Для небольшой внутренней команды возможна первичная защита на reverse proxy, с отдельной проверкой поведения OAuth callbacks.
2. Закрыть обход proxy: убрать внешнюю публикацию портов 8400/3400 либо привязать их к loopback. Не менять порты и firewall соседних сервисов.
3. Проверить конфигурацию и отрицательные сценарии в отдельной среде: без входа данные недоступны; с входом работают dashboard, API и задачи; OAuth reconnect не сломан.
4. Независимое ревью → исправления → подтверждающее ревью. Только после этого согласовать production rollout, доступ владельца и rollback; изменения поставлять через Git, не редактировать файлы VPS вручную.
5. Настроить SSH-ключ владельца/оператора, проверить второй вход, затем сменить переданный через чат root-пароль. Не отключать парольный вход до проверки ключа.

Рабочие контейнеры, БД, миграции, токены, рекламные настройки, root-пароль и соседние проекты не изменялись. Код на VPS не редактировался, pull/build/deploy не выполнялись.
