# HANDOFF 2026-08-30 — project bootstrap

## Done
- Spec received (docs/SPEC.md v1.0). Repo skeleton per SPEC §24, FastAPI /health /ready, worker placeholder, compose (postgres+redis+api+worker), smoke test.
- Repo `bandurkas/TT` (main). VPS3 `/root/TT` cloned via deploy key (github-tt), `docker compose build api` OK, ports 8400/3400 reserved. Containers NOT started (`.env` = copy of example, no secrets yet).

## Phase 0 next (SPEC §37/§39, in order)
1. TikTok Shop Partner Center: app + shop authorization + scopes → record in docs/tiktok-api-capability-matrix.md.
2. TikTok Ads developer app + advertiser auth; confirm GMV Max reporting.
3. Confirm Finance/Settlement/Payments scope for this seller.
4. Fill capability matrix from real responses (save fixtures).
5. Deliverables 2–4 (data-model, profit-calculation, attribution-model).
6. Deliverable 5 connectivity test; Deliverable 6 reconciliation vs Seller Center.

## Blockers / needs from user
- TikTok Shop app credentials + shop authorization (which shop: Lomira/Siluam socks?).
- TikTok Ads access token + advertiser_id.
- Telegram bot token / chat id.
- Initial SKU COGS table (per SPEC §3.4, versioned by date).
