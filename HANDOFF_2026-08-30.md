# HANDOFF 2026-08-30 (evening) — Phase 0 largely done, first live data pulled

## State
- Repo `bandurkas/TT` main @ 5368058+, 195 tests, ruff clean. VPS3 `/root/TT` deployed (api+caddy+postgres+redis up; worker is a placeholder). `https://tt.lomiraproduct.com` TLS OK.
- **TikTok Shop authorized**: seller Lomira.product (ID, LOCAL), shop_cipher in `.env` (local + VPS3), tokens in VPS3 `/root/TT/data/tiktok_shop_tokens.json` (0600). Access 7d / refresh 30d; `FileTokenProvider` auto-refreshes (flock).
- Live verification done → `docs/tiktok-api-capability-matrix.md` v0.3. Working: shops, orders, products, analytics 202509 (shop/video/video×product/product/sku), finance (statements, statement & order transactions, payments, withdrawals), returns. LIVE perf = TikTok internal error (0 LIVE GMV anyway).
- Deliverables: 1 (matrix, live) ✔, 2 data-model ✔ (31 tables, migration applied on VPS3), 3 profit-calculation ✔, 4 attribution-model ✔, 5 connectivity ✔ (probes; no persistent ingestion yet), 6 reconciliation ✔ (`docs/reconciliation-2026-08.md`: 12/12 August orders match API exactly).
- Code: finance engine (`src/analytics/profitability|attribution|transaction_types`), analytics (`baselines|creative_scoring|anomaly_detection|data_quality|reconciliation`), alerts engine, Shop/Ads/Telegram clients, daily report formatter, sync state. All reviewed (2 independent reviews + fixes), confirming review pending at time of writing.

## Key facts learned from live data
- Orders carry NO source video / creator → video→order attribution must be derived (video×product performance + time windows). `attribution_source=derived`.
- Shop performance: gross_revenue split GMV_MAX 92.9% / NON_GMV_MAX 7.1% (23–30 Aug); GMV by type VIDEO / PRODUCT_CARD / LIVE. → Ads API + honest adjusted ROAS is central.
- Analytics freshness: `latest_available_date` = D-1.
- Orders contain PII (buyer_email, recipient_address) → `default_redactor` strips before raw sink.
- Shop granted write scopes too (product.write, promotion.write…). Code never uses them; recommend disabling in Manage API.

## Finance model — live finding (changes Phase 2 design)
`GET /finance/202309/orders/{id}/statement_transactions` returns ONE flat record per order (+ `sku_statement_transactions[]`) with ~60 named amount fields (gross_sales, revenue, fee_amount, platform_commission, affiliate_commission, shipping_cost, shipping_fee, customer_shipping_fee, platform_shipping_fee_discount, platform_discount, refunds, taxes, adjustment, settlement_amount, statement_id, status). Identity observed: `settlement_amount = revenue_amount + fee_amount + adjustment_amount` (fee_amount aggregates components). → Replace native-type→NormalizedType guessing with a FIELD-based mapper (each field → sign/category). Fixture: `tests/fixtures/tiktok_shop/order_statement_transactions_settled.json`.

COGS confirmed 2026-08-30: 43 SKUs = 5-pair packs 25,000 IDR; 2 SKUs (kids 'Isi 10 Pasang') = 50,000; packaging 0, inbound logistics 0; effective from 2026-01-01 (assumed shop start). `seller_sku` empty on all 45 SKUs → cost keyed by TikTok sku_id. Seed: `seed/cogs_seed.csv` (4 SKUs with unknown pack size flagged). Telegram deprioritised — dashboard is the primary surface.

## Ingestion live (2026-08-30 late)
Phase 1 ingestion deployed and backfilled 60 days on VPS3 (`python -m apps.worker.cli backfill --days 60`; jobs: catalog, orders, order_statements, statements, withdrawals, video/product/sku/shop metrics; sync state in `integration_sync_state`, raw in `raw_api_responses`). DB after backfill: orders 86 (Aug: 85, statuses DELIVERED 43 / COMPLETED 29 / IN_TRANSIT 7 / CANCELLED 6), order_statement_records 28 SETTLED = 2,495,810, settements 29 (11 ad-deduction statements = −2,495,139 = Seller Center Adjustments ✔), payouts 16, videos 32, video_metrics 300, shop_metrics 60 (Aug GMV 5.49M: video 0.86M / product card 4.62M; GMV Max share 80–100% daily), COGS 45 versions. Finance mapper `src/analytics/finance_fields.py` (63 fields; 12/12 August through engine). Mapper for analytics datetime strings assumes shop tz (UNVERIFIED). No scheduler yet — run CLI by hand or add cron.

Dashboard mock-up (EN/RU) for the user: https://claude.ai/code/artifact/e6de4d2b-5344-4d90-a889-fab4bc80a08c — approved look; implement with Next.js per SPEC §41+.

## First profit computation (2026-08-31 01:10 WIB, VPS3, `profit_cli report --month 2026-08`)
Accrual by order date, 79 non-cancelled orders: net revenue 6,574,713 · fees 913,529 · COGS 2,200,000 · ads (BLENDED, LOW) 2,495,139 · **net profit +1,879,574 (28.6%)**. 28 settled / 52 provisional (fees estimated from trailing ratio). Cash view (Seller Center payout) is −93,654 because ads are charged immediately while settlements lag ~8–10 days → August is profitable on accrual, negative on cash. Caveats: (a) ad cost = all 11 adjustment-only statements; 6 match export "GMV payment for TikTok Ads" exactly (2,231,033), 5 others on Aug 1/2/10/11 (264,106) UNVERIFIED — ask user to check their type in Seller Center; (b) order 585309825279886780 (Aug 1) has line item with unknown SKU → COGS 0, flagged; add shop-level default COGS fallback (25,000/unit); (c) profit job pending confirming review. Migrations head c7f3a9d2e514.

## Next steps (in order)
1. Profit job: order_statement_records + sku records → finance_fields → order_profit → `analytics_order_profit` (versioned), COGS from sku_cost_versions; ad cost = ad-deduction statements allocated BLENDED per day; shop/day aggregates for dashboard.
2. Scheduler (APScheduler in apps/worker/main.py): orders+statements hourly, metrics daily 03:00 WIB, profit recompute after each.
3. Ad cost ingestion: statements with revenue=0 & adjustment<0 = GMV Max payout deductions (Aug total −2,231,033; shop payout −93,654 for the month). Match to export rows, then allocate to orders/days.
4. COGS table from user → `sku_cost_versions`.
5. Ads: app 'Lomira Profit Control' created 2026-08-30 at business-api.tiktok.com, **Verification Pending** (scopes: Ad Account Mgmt, Ads Mgmt, Reporting, Creative Mgmt, Onsite Commerce Store; redirect `/oauth/tiktok-ads/callback`). After approval: App ID/Secret → .env; implement auth_code→token exchange in `oauth.py` BEFORE user clicks authorize; then GMV Max report probe.
6. Telegram token → daily report job.
7. Then LLM layer + dashboard (SPEC §11–12, §41+).

## Needs from user
COGS table; Ads developer app; Telegram bot token; optionally disable write scopes.
