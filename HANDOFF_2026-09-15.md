# HANDOFF 2026-09-15 — TT / Lomira Profit Control

**Начинать отсюда.** Предыдущий: `HANDOFF_2026-09-01.md` (устарел в части Ads API — приложение одобрено).
Код: `~/dev/TT` = GitHub `bandurkas/TT` main = VPS3 `/root/TT`. Последний коммит — документ по связке.

---

## 0. Состояние на 15.09, проверено живьём

| что | статус |
|---|---|
| Сайт `tt.lomiraproduct.com` | 200, все контейнеры живы |
| Ads API (`ads_tiktok`) | ✅ каждые 15 мин, последний успех 02:30 UTC |
| Расход `shop_ad_days` | ✅ по 15.09 включительно (сегодня) |
| Заказы, товары | ✅ по 14.09 |
| Метрики видео | ✅ по 13.09 |
| **`video_product_metrics`** | ❌ **застрял на 01.09, 14 дней** — см. §4 |

**P&L по неделям (себестоимость уже правильная):**

| период | прибыль | реклама | заказы | маржа |
|---|---|---|---|---|
| август | +693 478 | 4 881 770 | 89 | +10,2% |
| 01–08.09 | −300 449 | 2 192 042 | 50 | −11,7% |
| **08–14.09** | **+176 239** | 763 405 | 31 | **+12,7%** |

Неделя 08–14 вышла в плюс после перестройки кампаний (§3).

---

## 1. Что сделано 09–10.09 (одна большая сессия)

### Ads API одобрен и подключён
- Приложение `Lomira Profit Control` (App ID `7683195109979537429`) одобрено. Ключи в `/root/TT/.env`
  (бэкап `.env.bak.20260909-1309`). Обмен `auth_code→token` отработал вживую впервые.
- Токен в token store (`data/tiktok_ads_tokens.json`), **не в `.env`** — `TIKTOK_ADS_ACCESS_TOKEN` там пустой, это норма.
- **Два кабинета, опрашивать оба:** `7658353454934671368` (Lomira.product — основной, все кампании)
  и `7658349761812004880` (только старая июльская). ⚠️ `/campaign/get/` для основного возвращает **0** —
  GMV Max кампании туда не попадают. Store `7494756467878495303`, BC `7658354375938932744`.
- Карта эндпоинта `/gmv_max/report/get/` с проверенными именами полей — `docs/ads-api-live-probe-2026-09-09.md`.
  Имена **не угадываются** (API отвечает `ERROR Message.`), взяты из документации Supermetrics.

### Расход из API вместо ручного ввода
- `src/integrations/tiktok_ads/gmv_max.py` + `src/domain/ads/tiktok.py` + джоб `ads_tiktok` (`*/15`).
- Закрытые дни `final`, текущий — `partial` с отметкой времени → на дашборде «на 20:49».
- Windsor автоматически отключается, пока Ads API авторизован (иначе два источника перетирали бы друг друга).
- **Сверка 20/20 закрытых дней до рупии** между API и продом; спорный период 22.07–03.08 (620 590)
  подтверждён официальным источником — поправка от 02.09 по Windsor была верной.
- Ручная форма `AdCost.tsx` удалена. Эндпоинт `/api/advertising/manual` оставлен для исправлений.

### Себестоимость
- Реальная: **2 500 за пару**. Через `/api/costs`: базовая партия 12 500 (5 пар) от 01.01 + переопределения:
  SATUAN (товар 51, 16 SKU) 2 500, детские (товары 6, 10) 25 000. Итог 55/16/2 SKU. 141 заказ пересчитан.
- Упаковка и входящая логистика = 0 — цифр не было. Если есть, маржа сдвинется вниз.
- **Август стал прибыльным** (+693 478); при старой себестоимости 25 515 считался −354 825.

### Вкладка кампаний
- `loaders.campaign_spend()` + `/analytics/campaigns` — реальный расход, заказы/выручка/ROI TikTok по кампаниям.
- Заказы и выручка — **атрибуция TikTok, не бухгалтерия магазина** (SPEC §7), подписано в UI.
- Атрибуция добрана с 01.05 окнами по 30 дней (лимит API на дневные запросы).

### Связка «видео → товар → расход» — `docs/creative-bridge-2026-09-10.md`
- **`item_group_id` в отчёте = `external_product_id` магазина.** Расход по товарам измеряется, не размазывается.
- Блендовое распределение врало до **3×** на товар (детским приписывали 141 542 вместо 50 443).
- Метрика **запас** = безубыточный CPO / фактический, порог у каждого товара свой
  (поштучная 18 918, комплект 33 007, детские 68 038). `≥1.2 scale`, `0.9–1.2 hold`, `<0.9 cut`.
- **Прибыли на видео нет намеренно:** GMV Max относит 96% заказов в `item_id = -1`.
  Видео судится по GPM и вердикту товаров, которые несёт.
- Расход по товарам в `ad_metrics(entity_type="product")`, история с 01.06 (668 записей).
- Эндпоинт `/analytics/creative-bridge`, компонент `Bridge.tsx`, вкладка «Видео → товар → расход».

### Диагностика кампаний (на 09.09, до перестройки)
- `docs/campaign-fix-instructions-2026-09-09.md` — три рекомендации с цифрами. Владелец перестроил кампании (§3).
- LIVE GMV Max: у всех identity `live_gmv_max_available: false` при `product_gmv_max_available: true`,
  эфиры при этом идут почти каждый день. **Причина не найдена**, отложено по решению владельца.
- Обучение: 7 дней типично, 5–14 разброс, **50 конверсий** порог; правка бюджета >20% сбрасывает.
  При ~6 заказах/день три кампании не выйдут из обучения никогда — тем более 14 (см. §3).

### Windsor как инструмент управления
- Через `mcp__claude_ai_Windsor_ai` для TikTok доступно: `pause/enable` campaign/ad group/ad,
  `set_campaign_budget`, `set_ad_group_budget`. **Создавать кампании нельзя.** Не применялось —
  CLAUDE.md требует read-only в MVP; любой write = policy engine + approval + audit log.

---

## 2. Итоги тестов и деплоя
439 тестов, `tsc` чист. Всё задеплоено. Коммиты: `f8ed12f` (Ads API + снос ручного ввода),
`c89769c` (вкладка кампаний), далее связка и документ.

⚠️ **Два раунда независимого код-ревью по флоу CLAUDE.md не проводились.** Самопроверкой поймано и
исправлено три реальных дефекта (конфликт Windsor/API, неполная атрибуция → ROI 0.4 вместо 1.77,
пропущенные поля вердикта у товаров без продаж). Ревью — первый пункт долга.

---

## 3. Кампании сейчас (08–14.09) — владелец перестроил после 10.09

| кампания | расход | зак | CPO | ROI |
|---|---|---|---|---|
| majority black | 448 919 | 21 | **21 377** | **2.93** |
| LIVE GMV Max_Gross revenue (новая) | 341 361 | 5 | 68 272 | 1.07 |
| **Smart_Growth_1736664758417392711** | **253 815** | **1** | 253 815 | **0.13** |
| **mix product** | **131 262** | **0** | — | 0 |
| moms and girls | 84 263 | 1 | 84 263 | 1.05 |
| Black Product | 60 501 | 2 | 30 250 | 1.82 |
| abu abu product | 48 331 | 1 | 48 331 | 1.25 |
| Smart_Growth_1736664922058228807 | 12 741 | 1 | 12 741 | 4.04 |
| ещё 6 Smart_Growth + Kids 10 pairs | < 21 000 | 0 | — | — |

**Что сработало:** бюджет majority black снижен со ~160k до ~64k/день → CPO упал с 36 954 до 21 377,
ROI вырос с 1.77 до 2.93. Ровно то, что предсказывало ценовое исследование.

**Что горит:** `Smart_Growth_1736664758417392711` (253 815 за 1 заказ) и `mix product` (131 262 за 0)
— **385 тысяч за неделю** в двух кампаниях. Это половина всего расхода.

**Структурная проблема:** 14 кампаний на ~4–5 заказов в день. Порог обучения 50 конверсий
недостижим ни для одной, кроме majority black. Дробление на «по кампании на товар» противоречит
выводу про обучение из §1. Стоит обсудить консолидацию.

---

## 4. Открытые проблемы

1. **`daily_metrics` падает с 02.09**: `metrics.video_product_metrics: TypeError: unsupported operand
   type(s) for +: 'int' and 'list'`. Метрики видео и товаров при этом пишутся (шаг раньше), падает
   только `video_product_metrics` → таблица на **01.09**. Следствие: **видео, опубликованные после
   01.09, не привязаны к товарам** в связке (`video_products` пополняется этим же шагом).
   Джоб помечается error, но `finance_cycle` ежечасно пересчитывает прибыль, так что P&L не страдает.
   **Чинить первым.** Файл: `src/domain/ingest/jobs.py`, функция синка `video_product_metrics` —
   API стал отдавать список там, где ждали число.
2. **Два раунда код-ревью** всего сделанного 09–10.09 (см. §2).
3. **Reported ROAS за период** — по кампаниям есть, к периоду не сведён: нужна миграция
   `analytics_shop_daily`. На дашборде «только по кампаниям».
4. **LIVE GMV Max** — причина `live_gmv_max_available: false` не найдена при живых эфирах.
   Проверить в Seller Center: есть ли LIVE GMV Max в типах кампаний, привязан ли Official Account
   с правом эфиров. Отложено владельцем.
5. Ревизия фронтенда за пределами рекламного контура (`Trend`, `Funnel`, `Opps`, `Board`,
   `Diagnosis`, `Orders`, `History`) не делалась.
6. Упаковка / входящая логистика в себестоимости = 0.

---

## 5. Что дальше — предложение

1. Починить `video_product_metrics` (§4.1) — без этого связка слепнет по новым видео.
2. Разбор с владельцем: два горящих кампании (385k/нед) и консолидация 14 → 3–4.
   Перераспределение по вердиктам связки: детские (запас ×2.69) и Unisex (×2.01) недофинансированы.
3. Код-ревью, два раунда.
4. Reported ROAS к периоду.

## 6. Как проверять
```bash
curl -s https://tt.lomiraproduct.com/health | python3 -m json.tool | grep -A6 ads_tiktok
curl -s "https://tt.lomiraproduct.com/api/analytics/creative-bridge?from=2026-09-08&to=2026-09-14"
curl -s "https://tt.lomiraproduct.com/api/analytics/campaigns?from=2026-09-08&to=2026-09-14"
```
Ручной прогон джоба: `docker compose exec -T worker python -c "from apps.worker import main; print(main.run('ads_tiktok'))"`.
Деплой: `git pull && docker compose build <svc> && docker compose up -d --force-recreate <svc>`.
