# HANDOFF 2026-08-31 — TT / Lomira Profit Control

**Start here.** Previous: HANDOFF_2026-08-30.md (bootstrap day, keep for history). Rules: CLAUDE.md. Spec: docs/SPEC.md.

**Accounting correction:** advertising figures below based on GMV Pay are superseded. GMV Pay confirms funding/payment, not campaign Cost. See [report-import.md](docs/report-import.md) for the daily Cost source, partial-day handling, migration and operator workflow. Do not reuse the previous profit totals as reconciled results.

## State in one paragraph (updated 31.08 evening, Jakarta)
Repo `bandurkas/TT` main @ `ff19589`+ (335 tests, ruff clean). VPS3 `/root/TT`: api (8400) + caddy (TLS) + postgres + redis + **worker = APScheduler** (finance :05 hourly, order_statements */6 :40, withdrawals */6 :20, daily_metrics 03:00 WIB; job state in `integration_sync_state` `job:*`, `/health` reports DB + jobs + syncs + stale). DB head `a9c4e7f1b2d3`. Shop API authorized; Ads app was **rejected 31.08** (communication email must be on the company-website domain) — user created info@lomiraproduct.com and updated/resubmitted the application same day; awaiting re-review. After approval: App ID/Secret → .env, then advertiser authorization click (auth_code→token already implemented in oauth.py, untested live). Dashboard mock approved (`docs/dashboard-mock.html`); **dashboard API live** under `/api/*` (docs/dashboard-api.md); Next.js frontend `apps/dashboard` being built (agent, uncommitted at time of writing — check `git status`).

## Done this session
- Profit job: 2 review rounds (16 findings closed + confirming review), deployed; default COGS 25,000 set (`profit_cli config --default-cogs`). August recomputed: **79 orders, net revenue 6,592,537, fees 895,705, COGS 2,225,000, ads 2,495,139 → net +1,872,398 (28.4%)**, 27 settled + 1 refunded / 52 provisional, mismatches 0; recompute is idempotent (unchanged=80).
- Scheduler + /health (2 review rounds), deployed, first finance_cycle ok.
- Dashboard API (SPEC §54): overview/trends/products/videos/campaigns/creators/funnel/insights/tasks + **`/api/analytics/video-products`** (videos → product cards: per video × product impressions/clicks/GMV from `shop_videos/{id}/performance` — live-verified; shop split video/product-card/live from shop_metrics; lag correlation views→card GMV; `history`: per-product daily video vs non-video GMV, publish events, 7d before/after lift verdicts; per-video daily series + rising/steady/fading). New tables `video_products`, `video_product_metrics` (job `video_product_metrics` in the daily metrics cycle, 1 call/video/day, 429s retried). Backfilled Aug 1–29.
- Facts learned: product performance API has NO impressions/clicks (product CVR = NOT AVAILABLE); video list API gives views + CTR only (clicks derived); video detail API gives real per-product impressions/clicks. August: video share of GMV 15.8% (0.86M of 5.49M); Pria Hitam gets 453,797 video GMV from videos …1922 and …0504.

## Open issues / caveats
1. ~~Ad-cost classification~~ **RESOLVED 31.08**: Seller Center export `income_20260830221550(UTC+7)` Reports sheet: “GMV payment for TikTok Ads” = −2,495,139 = Σ all 11 adjustment-only statements (incl. the 5 on Aug 1/2/10/11 = 264,106). All Adjustments in August are TikTok Ads; Article 22 tax (−2,500) is an in-order fee, not a statement. Payout −93,654, affiliate −54,315, refunds −391,000 also match.
2. Compare period = July has 1 order → "vs previous" deltas are meaningless until September.
3. All 21 videos classify INSUFFICIENT_DATA (ShopConfig min clicks 50 vs derived clicks ≤30) — consider lowering `minimum_sample_clicks` or scoring on the new real per-product clicks.
4. Funnel: only video traffic has impressions/clicks; order pipeline shown separately; settled stage excluded from deterioration.
5. Analytics datetime strings assumed shop tz — UNVERIFIED. 6. Write scopes granted to the shop app — recommend disabling.
7. Frontend agent output must be reviewed (independent review → fixes → confirming) before deploy; then Caddy routes `/` → dashboard:3400, `/api|/oauth|/health` → api.

## Next blocks
A. Finish + review + deploy `apps/dashboard` (Next.js, port 3400) — check `docker compose` service `dashboard`, Caddyfile.
B. Insights → tasks flow polish; recommendations table (SPEC §52) when LLM layer starts (only after B stable).
C. Ads API after approval (App ID/Secret → .env → `/oauth/tiktok-ads/callback` → per-campaign cost replaces BLENDED).
D. LLM narrative layer (SPEC §11–12).

## Ops cheatsheet (VPS3, /root/TT)
```
git pull && docker compose build api worker && docker compose run --rm worker alembic upgrade head && docker compose up -d --force-recreate api
docker compose run --rm worker python -m apps.worker.cli orders            # incremental
docker compose run --rm worker python -m apps.worker.cli order-statements
docker compose run --rm worker python -m apps.worker.cli statements
docker compose run --rm worker python -m apps.worker.cli metrics
docker compose run --rm worker python -m apps.worker.profit_cli compute
docker compose run --rm worker python -m apps.worker.profit_cli report --month 2026-08
docker compose run --rm worker python -m apps.worker.profit_cli config --default-cogs 25000
docker compose run --rm worker python -m apps.worker.cli video-products-backfill   # links from raw, no API
curl -s localhost:8400/oauth/tiktok-shop/status; curl -s localhost:8400/health
curl -s "localhost:8400/api/dashboard/overview?from=2026-08-01&to=2026-08-31"
docker compose logs --tail 50 worker   # scheduler
```
Secrets only in `/root/TT/.env` and `~/Desktop/TT/.env` (never in git). Probe scripts: scp to /tmp, run via `docker compose run --rm -T -v /tmp/x.py:/app/x.py worker python x.py`.

## Needs from user
- Ads app re-review result → App ID / Secret into `.env`.
- Optional: disable write scopes in Partner Center.
