# HANDOFF 2026-08-31 — TT / Lomira Profit Control

**Start here.** Previous: HANDOFF_2026-08-30.md (bootstrap day, keep for history). Rules: CLAUDE.md. Spec: docs/SPEC.md.

## State in one paragraph
Repo `bandurkas/TT` main @ `d966a0e`+ (291 tests, ruff clean). VPS3 `/root/TT` deployed: api (8400) + caddy (`https://tt.lomiraproduct.com`, TLS) + postgres + redis + worker (placeholder loop; jobs run via CLI). DB migrated to head `c7f3a9d2e514`, backfilled 60 days from TikTok Shop API and profit computed for August. Shop API authorized (Lomira.product, tokens on VPS3 `data/`, auto-refresh). Ads API app "Lomira Profit Control" created at business-api.tiktok.com — **Verification Pending**. Telegram deprioritised. Dashboard mock-up (EN/RU) approved by user: https://claude.ai/code/artifact/e6de4d2b-5344-4d90-a889-fab4bc80a08c

## Deliverables 1–6: all done
Capability matrix (live-verified), data-model, profit-calculation, attribution-model, connectivity, reconciliation (12/12 August orders exact vs Seller Center). See docs/.

## What the code does now
- `src/integrations/tiktok_shop` client (signing verified live), `tiktok_ads` client + OAuth exchange (untested live), telegram client.
- `src/domain/ingest` jobs + `apps/worker/cli.py`: catalog, orders, order-statements, statements, withdrawals, metrics(video/product/sku/shop), cogs, backfill, status. Raw responses kept in `raw_api_responses`; sync state in `integration_sync_state`.
- `src/analytics/finance_fields.py`: TikTok flat per-order statement record (63 fields) → FinanceTxn list; identity settlement = revenue + fee + adjustment; revenue-side mismatch guard.
- `src/analytics/profitability.py` engine (Decimal, versioned COGS, statuses), attribution, baselines, creative scoring, anomalies, data quality, reconciliation, alerts.
- `src/domain/profit` + `apps/worker/profit_cli.py`: order profit (versioned rows in `analytics_order_profit`), ad cost = adjustment-only statements BLENDED over trailing 7 days (confidence LOW), daily aggregates `analytics_shop_daily` / `analytics_product_daily`, `report --month`.

## August 2026 result (first computation, VPS3)
79 non-cancelled orders: net revenue 6,574,713 · fees 913,529 · COGS 2,200,000 · ads 2,495,139 → **net +1,879,574 (28.6%)** on accrual; 28 settled / 52 provisional (fees estimated). Cash (Seller Center payout) −93,654 because ads are deducted immediately while settlements lag ~8–10 days.

## Open issues / caveats (do these first)
1. **Confirming review DONE (after session cut-off)**: ingestion findings 15/15 CLOSED (nit: `_orders_needing_statement` bool_or checks only SETTLED, not PAID → a PAID-only order re-polls forever). Profit job verdict **FIX-THEN-SHIP** — apply, then confirming review, then deploy + recompute:
   - HIGH: `compute --since` loads orders from since−7d but allocates ALL deductions → over-allocation vs full run. Fix: with `since`, allocate only deductions with day ≥ since; persist only local_date ≥ since; look-back orders load-only.
   - HIGH: multiple settled statement records per order reduced to "latest wins" → a refund-only later statement would drop the sale. Fix: build txns from ALL settled records (ids already unique) so status can be ADJUSTED; warn when >1.
   - MED: snapshot hash includes warnings text + drifting fee ratio → version churn; hash only material inputs, quantize ratio.
   - MED: `TxnList.mismatch` discarded in build_txns → add to warnings + snapshot.
   - MED: unsettled placeholder records (real gross/fees) ignored for provisional orders → feed them to statement_record_to_txns (settlement_id None → PROVISIONAL) instead of ratio estimate; ratio only as fallback.
   - MED: engine exceptions (CurrencyMismatch/DuplicateTransaction) abort whole run → per-order try/except, count skipped.
   - MED: CLI report table lacks settled/provisional columns → add.
   - LOW: any negative adjustment-only statement counted as ad spend (Article 22 tax −2,500) — set `extra.classification` in map_statement / match known amounts; product_daily fee rounding drift → use allocate_proportionally; hash not invariant to Decimal exponent → normalize; stale product_daily rows not zeroed; per-unit line items same SKU map to first item; migration/doc header says "Revises: b4e7a2c91d03" (actual c7d2a9f14e58); tests missing for load_inputs/recompute_daily/since consistency/multi-record/mismatch/CLI.
2. Ad-cost classification: 11 adjustment-only statements = 2,495,139; 6 match Seller Center "GMV payment for TikTok Ads" exactly (2,231,033); 5 on Aug 1/2/10/11 (64,972; 29,353; 70,214; 55,062; 44,505 = 264,106) UNVERIFIED — **ask user** to check their type in Seller Center → Finance → Transactions.
3. Order 585309825279886780 (Aug 1) line item has SKU not in catalog → COGS 0 (flagged). Add shop-level default COGS fallback 25,000/unit (user: all packs are 5 pairs, kids 10-pair = 50,000).
4. Analytics datetime strings assumed in shop tz (Asia/Jakarta) — UNVERIFIED.
5. No scheduler: add APScheduler in `apps/worker/main.py` — orders+order-statements+statements hourly, withdrawals 6h, metrics daily 03:00 WIB, then `profit compute` + aggregates.
6. Shop granted write scopes (product.write, promotion.write…) — code never uses them; recommend user disables in Manage API.

## Next blocks (after 1–3)
A. Scheduler (5) + `/health` reporting last sync times.
B. Dashboard (SPEC §41–56) as Next.js in `apps/dashboard`, port 3400 behind Caddy at `/`; API endpoints SPEC §54 in `apps/api` reading `analytics_*` tables; EN/RU i18n like the mock; role views.
C. Ads: when TikTok approves → user puts App ID/Secret in `.env` → user clicks advertiser authorization → `/oauth/tiktok-ads/callback` stores token → probe `/report/integrated/get/` + GMV Max reports → per-campaign ad cost replaces BLENDED.
D. LLM layer (SPEC §11–12) only after B has reliable analytics output.

## Ops cheatsheet (VPS3, /root/TT)
```
git pull && docker compose build api worker && docker compose run --rm worker alembic upgrade head && docker compose up -d --force-recreate api
docker compose run --rm worker python -m apps.worker.cli orders            # incremental
docker compose run --rm worker python -m apps.worker.cli order-statements
docker compose run --rm worker python -m apps.worker.cli statements
docker compose run --rm worker python -m apps.worker.cli metrics
docker compose run --rm worker python -m apps.worker.profit_cli compute
docker compose run --rm worker python -m apps.worker.profit_cli report --month 2026-08
curl -s localhost:8400/oauth/tiktok-shop/status
```
Secrets only in `/root/TT/.env` and `~/Desktop/TT/.env` (never in git). Probe scripts: scp to /tmp, run via `docker compose run --rm -T -v /tmp/x.py:/app/x.py worker python x.py`.

## Needs from user
- Type of the 5 early-August adjustments (item 2).
- Ads app approval → App ID / Secret into `.env`.
- Optional: disable write scopes in Partner Center.
