# HANDOFF 2026-09-01 — TT / Lomira Profit Control

**Start here.** Previous: HANDOFF_2026-08-31.md (keep for history). Rules: CLAUDE.md. Spec: docs/SPEC.md.

## ⚠️ First thing in the new session
**Only one thing is left: deploy `dashboard` to VPS3.** The code is reviewed, fixed, browser-verified and pushed; VPS3 is the single stale piece.

```sh
ssh root@187.127.114.34
cd /root/TT && git pull --ff-only origin main   # remote is "origin"; github-tt is the SSH host alias, not a remote
docker compose build dashboard && docker compose up -d --force-recreate dashboard
curl -s localhost:8400/health          # backend untouched by this commit, should stay ok
```
Then smoke-test https://tt.lomiraproduct.com: zone 1 should show two new full-width panels — "Ad cost source / Enter today's ad Cost from Ads Manager" and "Product cost · FIFO" (Locked by default). No migrations and no api/worker rebuild are needed: `git diff ff0a181..HEAD -- src apps/api apps/worker migrations` is empty.

(The previous session could not run the deploy itself: the auto-mode permission classifier blocks state-changing SSH commands against the VPS. Read-only SSH is fine. If it blocks again, the user has to run the four lines above by hand or add a Bash permission rule.)

## Order count: dashboard vs TikTok — reconciled, no discrepancy
The user saw **10** in the cabinet and 8 on the dashboard. Both are right, they count different things. For 2026-09-01 (Jakarta), live API: **8 orders / 10 units**. Two of the eight orders carry two items each (`585834738001610193` and `585829667896133568`, each 1 x SKU `...934791` + 1 x SKU `...205831`). The user's Ads Manager screenshot labels its figure **"SKU orders: 10"** — that is units, which matches ours exactly. Orders sync had run 1 minute before the check, so nothing was missing. `costs` in the order journal = 250,000 = 10 x 25,000, confirming 10 units.

Same screenshot (≈21:00 WIB): Cost **450,000**, SKU orders 10, cost per SKU order 45,000, gross revenue 810,904, ROI 1.80. The DB still holds the 20:00 entry of **421,192**, so a fresh manual entry is due — it could not be posted from that session (the permission classifier blocks writes to the VPS, same as the deploy). Ads Manager "gross revenue" 810,904 vs our GMV 825,000 is expected: different attribution windows, not an error.

## New: "What if" product-cost calculator (commit `4650e86`)
The user could not find where to enter a per-piece product cost — partly because **the whole cost panel is still undeployed**, and partly because the existing field is per *unit* (a 5-pair set at 25,000), not per piece, and it writes rather than previews. Added a what-if block to `Costs.tsx`: pieces-per-unit x cost-per-piece, preset prices coloured against break-even (green below, red above), and the result as three figures (product cost for the period, net profit, net margin) each with the current value and delta beneath. Break-even is shown per unit and per piece. "Apply as default cost" commits it when unlocked.

Math is anchored on the real KPI cards, not on rounded per-unit averages: `newNet = net_profit + (cogs_per_unit − newCogsPerUnit) × units`, margin over `net_seller_revenue`. Revenue, fees and advertising stay at the period's actual figures. One price is applied to every SKU, so it is labelled an estimate.

Today at ad Cost 450,000 (once entered), 10 units, net seller revenue 666,200: current 25,000/set → **−33,800 (−5.07%)**; break-even **4,324 per piece**; at 3,000/piece → **+66,200 (+9.94%)**.

## ⚠️ Manual ad-Cost entry has corrupted live data twice — fix before trusting the dashboard
Ads Manager reports **the selected date range**; `POST /api/advertising/manual` takes **one day**, and a newer observation silently replaces the old one. On 2026-09-01 that misfired twice within an hour:
1. A test entry of `2000` (sku_orders 0, gross 0) replaced the real 450,000 → the dashboard showed **+414,200 profit, +62% margin**.
2. Period totals `5,062,670 / 87 sku_orders / 8,636,752` were entered as "1 September" → **−4,646,470, −697% margin**. The shop actually did 8 orders / 10 units / 825,000 GMV that day, so these were plainly a month's figures.

Both restored by re-posting the real day (`450000 / 10 / 810904` → −33,800, −5.07%); superseded `SourceReport` rows remain in the audit trail, so nothing was lost.

**Proposed guard (not built, awaiting the user's go-ahead).** Two deterministic checks in `record_manual_ad_day()`, returning 422 with a clear message unless a `confirm` flag is set:
- entered `sku_orders` or `gross_revenue` exceeds the day's own actual units / GMV by more than ~2x → "these look like period totals, not one day";
- the new cost is a small fraction of the observation it would replace, or replaces non-zero values with zeros → "this would overwrite a fuller record".
Do **not** solve it by splitting a range across days: allocating a period's Cost per day is an invented number, which SPEC §2 and CLAUDE.md forbid. The right UX is to make the single-date scope unmissable in the form and to refuse implausible values.

## Verified this session (the two open checks are now closed)
1. **Panels render — ✅ confirmed in a real browser.** Headless Chromium against `MOCK=1 next dev` + the committed fixtures: both panels present, **zero console/page errors**, the Locked checkbox actually gates editing (Add-lot form and Edit/Deactivate/Set buttons only appear unlocked), lots table shows `32 / ∞` consumed/remaining and active/queued chips, per-SKU table expands its history, RU and EN both render.
2. **Refresh — ✅ genuinely refreshes everything.** One click issues **12 of 12** zone requests: overview, insights, trends, funnel, products, videos, campaigns, creators, video-products, tasks, orders, costs. Mechanism: `page.tsx` bumps `tick`; every `useApi` hook is keyed on it, including the order journal and the costs panel. Order counts come from both `overview` and `/api/orders` — both refetch.
   **Caveat worth telling the user:** Refresh re-reads the database. It does **not** re-sync from TikTok and does **not** force a server-side recompute — there is no such endpoint. Recompute happens on every Apply in the two panels (synchronously, all orders) and on the worker schedule. If the user wants the button to force a recompute, that is a new backend endpoint + wiring.

## Also fixed this session (commit `9035e55`)
- **Stale zones after an Apply (real bug).** `onAdApplied`/`onCostApplied` refetched a hand-picked subset, but every write recomputes *all* orders server-side — trends, campaigns, creators, orders and tasks stayed stale. Both callbacks now bump the global tick.
- **`lot.shared_skus` was invisible.** The API note rendered inside the panel says "see lot.shared_skus", but the UI never showed it and the TS type omitted it. Now rendered under the scope cell as "shared batch across N SKU" / «общая партия на N SKU».
- **Refresh wiped an in-progress edit.** `Costs.tsx` reset the default-cost input from every refetch; now it only syncs while the panel is locked.
- **RU plural.** "33 заказов пересчитано" → «пересчитано заказов: 33» (`recomputedText` in `lib/orders.ts`, used by both panels).
- **Layout.** Both panels sat side-by-side in `.two`, so their 6-column forms and 7/8-column tables were clipped at 1500px (Gross revenue, Note and the consumed/remaining column were cut off). Stacked full-width via a new `.stack` class.
- Known, **not** fixed (judgement call, flagging for the user): both panels live inside Health's `ov &&` branch, so if `/api/dashboard/overview` errors the operator loses the very form needed to correct ad-cost data. Given the `null.startsWith` crash earlier the same day, consider rendering them independently of overview.
- Dev-only nit, not fixed: the MOCK route's "newer observation" guard compares `prior.observed_at > now`, which can never be true, so that 422 path is unreachable locally. The real backend rule is correct.

## 🔴 Infra: the repo moved off iCloud — `~/Desktop/TT` → `~/dev/TT`
This was the root cause of a whole class of "flaky build" symptoms, and it had already done real damage.
- `brctl status` showed iCloud actively syncing `apps/dashboard/node_modules` (~180 MB, tens of thousands of files).
- Damage found: `.git/refs/heads/main` was **missing**, left as an unrenamed `main.lock`, so every git command answered *"branch main does not have any commits yet."* The lock held the correct sha (`8115ad3` = `origin/main`) and the object was intact, so it was recovered by renaming the lock back to the ref. Nothing was lost.
- Measured: copying `node_modules` off Desktop ran at ~100 KB / 20 s; `next dev` bound port 3400 but never answered a request, even after 2 minutes. After the move: `npm ci` 9 s, `next dev` ready in 2.3 s, `npm run build` green on the **first** attempt in 33 s (it used to need 2–3 tries).
- `.venv` and `node_modules` were deleted before the move and reinstalled at the new path. Paths updated in `CLAUDE.md`, `HANDOFF_2026-08-31.md` and the project memory file.
- **Other projects still live on `~/Desktop` (BUBU, Jony, …) and carry the same risk** whenever they hold a large dependency tree. Worth moving them too, or excluding those folders from iCloud.

The full spec for both features is at the bottom of this file ("Spec: manual ad-Cost entry + cost lots UI") if anything needs rebuilding.

## State in one paragraph
Repo `bandurkas/TT` main @ **`9035e55`** (385 backend tests + 10 node tests, ruff clean, typecheck/lint/build green). **local = GitHub = `9035e55`; VPS3 is behind at `ff0a181`** — backend code is byte-identical, only the dashboard UI is undeployed. Local checkout now at **`~/dev/TT`** (no longer on Desktop/iCloud). VPS3 `/root/TT`: api (8400) + caddy (TLS) + postgres + redis + dashboard (3400, Next.js) + worker (APScheduler: finance :05 hourly, order_statements */6 :40, withdrawals */6 :20, daily_metrics 03:00 WIB). DB head `c3f8a1e6b904`. `/health` → ok, all 4 jobs success, all 10 syncs success. Dashboard live at **https://tt.lomiraproduct.com**.

## What shipped today (2026-09-01)
- **Dashboard crash fix**: September's default period has no ad-Cost report → `unit_economics.ad_cost_per_unit = null` crashed `Health.tsx` (`null.startsWith`). Null-guarded, redeployed, verified live with headless Playwright across 5 views (commit `08fc479`).
- **Ads app rejected**: TikTok requires a communication email on the company-website domain. User created `info@lomiraproduct.com` (MX already on Hostinger) and resubmitted the same day (`fb4febf`, `9bd52b0`). **Awaiting re-review** — this is the blocker for live per-campaign ad data.
- **Manual daily ad-Cost import** (until Ads API works): confirmed with the user this is wanted as a *standing* feature, not a one-off — every time an ad-cost screenshot/export is available, transcribe it in. Two same-day imports done live from Ads Manager screenshots (17:00 and ~20:00 WIB): Sep 1 → 7 orders, 9 units, net revenue 601,599, ad Cost 421,192, **net profit −44,593 (−7.4%)**, blended ROAS 1.43. Day still partial/open.
- **Backend: manual ad-Cost entry + product cost lots (batches)** — `088504f` then review fixes `ff0a181` (independent review ran, FIX-THEN-SHIP → all 4 findings closed, confirmed by full suite):
  - `record_manual_ad_day()` in `src/domain/reports.py`: operator-entered daily Cost, same audit trail as XLSX imports (hashed `SourceReport`, only a *newer* observation may replace a day, `final=True` closes a day). `shop_ad_days.manual` flag (migration `c3f8a1e6b904`) so per-order snapshots correctly say `ad_source=manual_entry_cost` vs `campaign_overview_cost`.
  - `src/domain/costs.py` (new): purchase-lot model (`cost_batches` table, migration `b7e2c4d9a1f5`) → pure-FIFO `segments_for_sku()` → regenerated `sku_cost_versions`. A `scope=all`/`scope=product` lot's `quantity` is a **shared batch**: consumed once against the *combined* sales of every SKU it covers (not per SKU — this was a review finding, fixed). Seed/manual cost versions are clamped around a SKU's first lot (`effective_to` = lot start) instead of colliding, so version selection stays deterministic. **The worker scheduler now calls `rebuild_cost_versions()` at the top of every `_compute_profit()`** (also a review fix — without it, lots would freeze once exhausted and silently stop advancing).
  - API: `GET/POST /api/advertising`, `/api/advertising/manual`; `GET /api/costs`, `POST /api/costs/lots`, `PATCH /api/costs/lots/{id}`, `POST /api/costs/default` — all in `apps/api/dashboard.py`. Every write recomputes profit + aggregates synchronously and returns `recomputed: {orders, inserted}`.
  - **Deployed to VPS3**, migrations applied, `/api/costs` returns the 45 real SKUs at seed cost 25,000 (no lots entered yet — real usage starts once the frontend panel exists).
- **Reconciliation**: user's `income_20260830221550(UTC+7)` export confirmed all 11 August adjustment-only statements = TikTok Ads (2,495,139) — no unresolved classification questions left for August.
- **Parallel session** (codex, not this one — commits `fe1b2a5`..`f7376c3`) shipped a *separate*, more rigorous ad-Cost pipeline (`source_reports`/`shop_ad_days`, `report_cli`, order-level finance ledger `Orders.tsx`) that this session's manual-entry/cost-lots work builds directly on top of. **August's true profit is by reported Cost, not GMV Pay**: Cost 4,347,403 → **+82,871 net (1.2% margin)**, not the earlier +1.87M (28.4%) which used GMV Pay. Explained to the user (081M vs 4.35M IDR, GMV Pay = payment timing, not ad consumption) — see `docs/report-import.md`, `docs/deploy-ad-cost-20260831.md`.
- **User request answered, not yet built**: "why is 25,000 COGS, and it may drop, and we'll have several batches at different prices" → this is exactly what the cost-lots backend (`src/domain/costs.py`) now supports. User separately confirmed the desired UX: a locked/read-only panel by default, an unlock checkbox to edit, apply, re-lock automatically. That UX is what the frontend agent (see ⚠️ above) is implementing.

## Open issues / caveats
1. **Ads app re-review pending** (info@lomiraproduct.com, resubmitted 2026-08-31). Once approved: App ID/Secret → VPS3 `.env`, then user clicks advertiser authorization (`auth_code→token` exchange already implemented in `apps/api/oauth.py`, still untested live) → per-campaign/per-video ad cost replaces BLENDED/manual-Cost.
2. **Manual ad-Cost entry is a standing workflow now**, not deprecated — the user will keep pasting Ads Manager screenshots until the API works. The dashboard needs the entry form (see ⚠️ above) so this doesn't require an engineer to run a CLI script each time.
3. Frontend gap: the September default view previously crashed (fixed), but no lot/cost UI exists yet, so `/api/costs` and `/api/advertising/manual` are currently only reachable via curl/CLI. Low value to the user until the UI lands.
4. All videos still classify `INSUFFICIENT_DATA` (min-clicks threshold 50 vs realistic derived-click volumes) — cosmetic, not addressed this session.
5. July had 1 order — period-over-period comparisons are meaningless until there's a full comparable month (September vs August will be the first useful one).
6. Analytics datetime strings still assumed shop-tz — UNVERIFIED (carried over, low risk).
7. Shop API has unused write scopes (`product.write`, `promotion.write`, …) — recommend disabling in Partner Center (carried over, not urgent).

## Next steps (in order)
A. **Deploy `dashboard` to VPS3** (four lines at the top of this file), then smoke-test the live URL. Nothing else blocks.
B. From then on the user enters Ads Manager figures in the UI instead of curl — the manual-Cost form is the standing workflow until the Ads API is live. Keep the CLI/curl path in the Ops cheatsheet as a fallback.
C. Enter the real purchase lots in "Product cost · FIFO". This is what the user asked for ("why is COGS 25,000, it may drop, and we'll have several batches at different prices") — the backend has held the data model since `ff0a181` and the UI now exposes it, but no real lot has been entered yet: `/api/costs` still shows all 45 SKUs at the seed 25,000.
D. When Ads app is approved: wire live Ads API, retire manual entry as the primary source (keep it as a fallback), re-verify `oauth/tiktok-ads` live.
E. Optional, from the review: render the two panels independently of `overview` so an overview error cannot hide them; decide whether Refresh should also force a server-side recompute (new endpoint).
F. Later: SPEC §52 recommendations/tasks polish, LLM narrative layer (only after A is stable, per SPEC §37/39 ordering).

## Ops cheatsheet (VPS3, /root/TT)
```sh
git pull && docker compose build api worker dashboard && docker compose run --rm worker alembic upgrade head && docker compose up -d --force-recreate api worker dashboard
docker compose run --rm worker python -m apps.worker.profit_cli compute
docker compose run --rm worker python -m apps.worker.profit_cli report --month 2026-09
curl -s localhost:8400/health
curl -s "localhost:8400/api/dashboard/overview?from=2026-09-01&to=2026-09-01"
curl -s "localhost:8400/api/costs?from=2026-09-01&to=2026-09-01"
curl -s "localhost:8400/api/advertising?from=2026-09-01&to=2026-09-01"

# manual ad-Cost entry from an Ads Manager screenshot (no UI yet — see spec below):
curl -s -X POST localhost:8400/api/advertising/manual -H 'content-type: application/json' \
  -d '{"date":"2026-09-01","cost":"421192","sku_orders":9,"gross_revenue":"735034","final":false,"note":"screenshot 20:00 WIB"}'

# offline XLSX import path (still works, used by the parallel session's pipeline):
docker compose run --rm worker python -m apps.worker.report_cli ads '/app/data/reports/<file>.xlsx' --shop-id 1 --timezone Asia/Jakarta --observed-at <ISO8601+tz>
```
Secrets only in `/root/TT/.env` (never in git). VPS3 creds: see memory `reference_vps3.md`.

## Needs from user
- Nothing blocking right now except the TikTok re-review (external, no action needed from us until it resolves).
- Keep sending Ads Manager screenshots through the day if they want intraday profit numbers before the API is live.

---

## Spec: manual ad-Cost entry + cost lots UI (given to the frontend agent 2026-09-01, for reference/resume)

**A) Manual daily ad Cost** — in zone 1 next to `AdvertisingSource`, a compact form "Enter today's ad Cost from Ads Manager" / RU «Внести расход на рекламу из Ads Manager»: date (default today, shop tz), Cost (IDR), SKU orders, Gross revenue, checkbox "Day complete (final figures)" / «День закрыт (итоговые цифры)», optional note, Apply/«Применить» button.
`POST /api/advertising/manual` `{date, cost: string, sku_orders, gross_revenue: string, final: bool, note}` → 201 `{report_id, partial, recomputed:{orders, inserted}, day:{date,cost,source,partial,...}}`; 422 with `detail` string (newer entry exists / future date / final on unfinished day) shown inline. On success refetch overview/trends/insights/funnel/video-products.
Also render a per-day table from `GET /api/advertising?from&to` (`days[].source` = `"shop_overview"|"manual_entry"`, `days[].observed_at`, `days[].note`, `manual_days`, `source` label, `entry_note`): Date · Cost · SKU orders · Gross revenue · Source chip ("Campaign overview export"/"Manual entry" · RU «выгрузка»/«ручной ввод») · Partial flag · observed time. Keep honesty labels: manual = operator-transcribed, replaced only by a newer observation, superseded automatically once Ads API connects.

**B) Product cost (COGS) with a lock** — in zone 1 unit-economics card, a "Product cost" panel from `GET /api/costs?from&to` → `{default_cogs_per_unit, lots[], skus[], note}`. `lots[]`: `{id, scope "all"|"product"|"sku", product_id, sku_id, received_on, unit_cost, quantity|null, currency, note, active, consumed?, remaining?, shared_skus?}`. `skus[]`: `{sku_id, external_sku_id, product_id, product_title, sku_title, current_cost, source "lot"|"seed"|"default"|"none", lot_id, effective_from, history[]}`.
UX per client's explicit ask: checkbox "Locked" (checked by default, fields read-only) → uncheck to edit → (1) default cost per unit → `POST /api/costs/default {default_cogs_per_unit}`; (2) lots list: add (scope + product/sku picker from `skus[]`, received_on, unit_cost, quantity optional, note) → `POST /api/costs/lots` (201 `{lot_id, versions, skus_with_lots, recomputed}`); edit/deactivate → `PATCH /api/costs/lots/{id} {received_on?, unit_cost?, quantity?(0=clear), note?, active?}`. On success: "Applied — N orders recomputed", auto re-lock, refetch overview/products/funnel/video-products. Explain FIFO from the API `note` (a shared-scope lot's quantity is consumed once across ALL its SKUs' combined sales — `shared_skus` shows how many; a lot without quantity applies until the next one; a lot's seed price still covers time before its first lot). Status chips: "queued" (no consumed/remaining), "active" (current source), "sold out" (remaining=0). Per-SKU table with expandable history. RU strings throughout. Fixtures `advertising.json`/`costs.json` were being added (see uncommitted files above — verify their numbers match the shapes here, they may already be usable).
